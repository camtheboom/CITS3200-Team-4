import React from 'react'
import { View, Text, StyleSheet, Button, Alert, Modal , TextInput, TouchableOpacity } from 'react-native';
import { NavigationContainer } from '@react-navigation/native';
import { createNativeStackNavigator } from '@react-navigation/native-stack';

function Settings() {
    // I need a 'download data' button here
    // I will also need a button that links to a page to specify when to notify the user
    return (
        <View style={{alignItems:'center', justifyContent:'center', flex:1}}>
        <Text>Settings Screen</Text>
        <View>
            
        </View>
        </View>

    )
}
export default Settings